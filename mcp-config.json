{
  "_comment": "BATCAVE MCP config template. Copy this block into VS Code Claude Code settings under mcpServers. Replace <BATCAVE_DIR> with the absolute path to your batcave directory. Server name should match mcp.serverName in batman.config.json.",
  "batman-batcave": {
    "command": "node",
    "args": ["<BATCAVE_DIR>/v3/@claude-flow/cli/bin/cli.js", "mcp", "start"],
    "env": {
      "ANTHROPIC_API_KEY": "",
      "CLAUDE_FLOW_AUTO_UPDATE": "false",
      "CLAUDE_FLOW_DEBUG": "true",
      "NODE_ENV": "development"
    }
  }
}
