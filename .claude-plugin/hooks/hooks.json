{
  "_comment": "SENTNL SANDBOX: PreToolUse hooks REMOVED (no command interception). Stop hook REMOVED (no session tracking). Only safe read-only PostToolUse metrics kept.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[sandbox] bash command logged'"
          }
        ]
      }
    ]
  }
}
